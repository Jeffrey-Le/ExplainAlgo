import {useEffect} from 'react';

const useSignInPage = () => {
    useEffect(() => {
        // Fetch Data
    }, []);

    useEffect(() => {
        // Send Analytics Events
    }, []);

    // Other Effects
    // Probably just rendering effects
}

export {useSignInPage};