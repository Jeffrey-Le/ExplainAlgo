# !! DO NOT commit real credentials here !!
# This is a placeholder showing the secret shape.
# In practice this is generated by sealed-secrets or
# External Secrets Operator (tackled in step 4).
#
# To create manually for local k8s testing:
#   kubectl create secret generic postgres-secret \
#     --from-literal=username=explainalgo \
#     --from-literal=password=<your-password> \
#     --from-literal=url=postgresql+asyncpg://explainalgo:<password>@postgres:5432/explainalgo

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  username: REPLACE_ME
  password: REPLACE_ME
  url: REPLACE_ME

---
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:
  jwt-secret: REPLACE_ME
  gemini-api-key: REPLACE_ME
  redis-url: REPLACE_ME
